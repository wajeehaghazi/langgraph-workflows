# LangGraph Workflows Project

This project demonstrates the implementation of three core LangGraph workflow patterns:

1. Sequential Workflow with Conditional Routing
2. Parallel Workflow with Aggregation
3. Router Workflow using Structured Output

The system uses OpenAI-compatible models through LangChain and is managed using the `uv` package manager.

